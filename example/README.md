The code in here mirrors the output of `pubviz -o print` run on this package
with a couple of tweaks to run locally.

## Historical

The `viz-lite.js` and `viz.js` files are here just to enable older versions of
`pubviz` to continue working. The current version uses a CDN-hosted version of
`graphviz`.
